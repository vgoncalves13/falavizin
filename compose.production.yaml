services:
  web:
    image: ${APP_IMAGE:-ghcr.io/vgoncalves13/falavizin:latest}
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-8082}:80"
    env_file:
      - .env
    environment:
      DB_HOST: mysql
      APP_ENV: production
      APP_DEBUG: "false"
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - storage:/var/www/html/storage
    command: >
      sh -lc '
      mkdir -p storage/app/public storage/framework/cache storage/framework/sessions storage/framework/views bootstrap/cache &&
      chown -R www-data:www-data storage bootstrap/cache &&
      [ -L public/storage ] || php artisan storage:link &&
      php-fpm -D &&
      exec nginx -g "daemon off;"'

  worker:
    image: ${APP_IMAGE:-ghcr.io/vgoncalves13/falavizin:latest}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_HOST: mysql
      APP_ENV: production
      APP_DEBUG: "false"
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - storage:/var/www/html/storage
    command: >
      sh -lc '
      mkdir -p storage/app/public storage/framework/cache storage/framework/sessions storage/framework/views bootstrap/cache &&
      chown -R www-data:www-data storage bootstrap/cache &&
      [ -L public/storage ] || php artisan storage:link &&
      exec su -s /bin/sh www-data -c "php artisan queue:work --sleep=3 --tries=3 --timeout=120 --max-time=3600"'

  scheduler:
    image: ${APP_IMAGE:-ghcr.io/vgoncalves13/falavizin:latest}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_HOST: mysql
      APP_ENV: production
      APP_DEBUG: "false"
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - storage:/var/www/html/storage
    command: >
      sh -lc '
      mkdir -p storage/app/public storage/framework/cache storage/framework/sessions storage/framework/views bootstrap/cache &&
      chown -R www-data:www-data storage bootstrap/cache &&
      [ -L public/storage ] || php artisan storage:link &&
      while true; do su -s /bin/sh www-data -c "php artisan schedule:run --no-interaction"; sleep 60; done'

  mysql:
    image: mysql:8.4
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:${DB_FORWARD_PORT:-3309}:3306"
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root}
    volumes:
      - mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "MYSQL_PWD=\"$${MYSQL_ROOT_PASSWORD}\" mysqladmin ping -h127.0.0.1 -uroot --silent"]
      interval: 10s
      timeout: 5s
      retries: 12

volumes:
  mysql:
  storage:
